# OTTCOUTURE TM Slots

Hier landen alle hochgeladenen Teachable-Machine-ZIPs aus dem Konfigurator (`/config`). Jede ZIP muss folgende Dateien enthalten:

- `metadata.json`
- `model.json`
- `weights.bin`

Wir legen das entpackte Modell in einem eigenen Unterordner (Slug des angegebenen Namens) ab und dokumentieren es zusätzlich in `registry.json`. Über die Config-Seite lässt sich jedes Modell als Standard markieren (wir speichern die Auswahl serverseitig, damit der Analyzer automatisch darauf zugreift).
Wir legen das entpackte Modell in einem eigenen Unterordner (Slug des angegebenen Namens) ab und dokumentieren es zusätzlich in `registry.json`. Bitte füge hier deine Startermodelle ein (z. B. `starter-trichome` oder `starter-health`) oder nutze den Upload-Flow für neue Varianten.

Community-Hinweis:
- Rechte & Branding: [ottcouture.eu](https://ottcouture.eu)
- Feedback: otcdmin@outlook.com · Instagram @ottcouture.eu · Discord https://discord.gg/GMMSqePfPh
